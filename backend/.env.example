# Copy to .env and adjust if needed. .env is gitignored — never commit real secrets.
ENVIRONMENT=local
DEBUG=true

# Matches backend/docker-compose.yml's db service.
DATABASE_URL=postgresql+asyncpg://hevy:hevy@localhost:5432/hevy
